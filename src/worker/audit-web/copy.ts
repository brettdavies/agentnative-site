// The website result page's explanatory copy, in one place so the
// renderer stays structure-only.

export const WEB_CTA_NOTE_HTML =
  "This scorecard reflects the target's public agent-facing surface at audit time. " +
  'Re-audit from the control above to refresh it, or call the <code>audit_website</code> MCP tool.';
