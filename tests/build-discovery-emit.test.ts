// Placeholder scaffolded in U2; populated in U6 when
// src/build/11a-discovery-emit.mjs lands. Splitting the file creation
// from the assertions keeps the U6 diff focused on the emit logic
// itself rather than mixing in test-harness scaffolding.

import { describe, test } from 'bun:test';

describe.skip('.well-known/mcp + ai.txt + security.txt emit (U6)', () => {
  test('placeholder so the file exists at U2', () => {
    // Filled in U6 alongside src/build/11a-discovery-emit.mjs.
  });
});
